#!/bin/bash
#
# onodera - onodera at openmailbox dot org
# A script to easily play and manage your animu/kdrama/etc.

# TODO: Disable config.bak
# TODO: Remove "tr -d "\r""s?
# TODO: neet -a add escpism to all catagories

foreground="\e[0;39m"
brown="\e[1;33m"
red="\e[1;31m"

info() {
	list="$(cat "$HOME/etc/neet/list")"

	active="$(echo "$list" | grep "+")"

	name="$(echo "$active" | cut -d "|" -f 2- | cut -c 2- | tr -d "\r")"
	if [[ -z "$current" ]]; then
		current="$(echo "$active" | cut -d "/" -f 1 | cut -d "[" -f 2-)"
	fi
	total="$(echo "$active" | cut -d "]" -f 1 | cut -d "/" -f 2-)"
}

fuzzy() {
	directories="$(ls -d "$HOME/tmp/downloads/"*/ | cut -d "/" -f 5- | cut -d "/" -f 2-)"

	if [[ "$debug" == true ]]; then
		IFS=$'\n'
		for directory in $directories; do
			for word in $name; do
				# TODO: should IFS be unset here? seem logical but works better with set IFS...
				# unset IFS
				mostlikelydebug="$(echo "$directory" | grep -i "$(echo "$word" | tr -d ",")")"
				if [[ "$?" -eq 1 ]]; then
					echo -e "    $directory = ${brown}no$foreground"
				else
					echo -e "    $directory = ${red}yes$foreground"
				fi
				if [[ -n"$mostlikelydebug" ]]; then
					mostlikely=$mostlikely$mostlikelydebug
				fi
			done
		done
		unset IFS
	else
		mostlikely="$(
			IFS=$'\n'
			for directory in $directories; do
				# TODO: should IFS be unset here? seem logical but works better with set IFS...
				# unset IFS
				for word in $name; do
					echo "$directory" | grep -i "$(echo "$word" | tr -d ",")"
				done
			done
			unset IFS
		)"
	fi

	count="$(echo "$mostlikely" | uniq | wc -l)"
	if [[ "$count" -ge 2 ]]; then
		if [[ -n "$(grep "$name" "$HOME/etc/neet/save")" ]]; then
			mostlikely="$(cut -d "|" -f 2- "$HOME/etc/neet/save" | cut -c 2-)"
		else
			echo -e "There are $count directories that match $brown$name$foreground:\n" 
			# TODO: Add grep color here like in aiko
			echo -e "$(echo "$mostlikely" | uniq | nl | sed "s/^ */  /g")\n"
			read -p "Which directory would you like to use? [1-$count] " answer
			case "$answer" in
				[1-$count])
					mostlikely="$(echo "$mostlikely" | cut -d $'\n' -f $answer)"
					echo ""
					echo -e -n "Would you like to make this the default answer for $brown$name$foreground? [Y/n] "
					read -p "" answer
					case "$answer" in
						[Nn*])
							echo ""
							;;

						*)
							# TODO: Doesn't support multiple shows/lines
							echo "$name | $mostlikely" > "$HOME/etc/neet/save"
							echo ""
							;;
					esac
					;;
				*)
					exit
					;;
			esac
		fi
	elif [[ "$count" -eq 0 ]]; then
		echo "No match found."
		exit
	fi

	path="$(echo "$mostlikely" | uniq -c | sort | tail -n 1 | cut -d " " -f 8-)"

	if [[ "$current" -eq 0 ]]; then
		currentmpv=01
	elif [[ "$current" -le 9 ]]; then
		for number in {1..9}; do
			if [[ "$number" -eq "$((10#$current))" ]]; then
				currentmpv=0$number
			fi
		done
	else
		currentmpv="$current"
	fi
}

play() {
	echo -e "Playing episode $current/$total, $brown$name$foreground."
	mpc -q pause
	mpv --fullscreen "$HOME/tmp/downloads/$path/"*{E,e,Ep,ep,\ }{$currentmpv,$current}*{.avi,.mkv,.mp4}
}

shopt -s extglob

if [[ "$#" -ge 1 ]]; then
	case "$1" in
		-h|--help)
			echo "-a         add escapism"
			echo "-c escp    set current escapism"
			echo "-d         debug info"
			echo "-e         edit list"
			echo "-s         sort list"
			echo "+(++...)   increment ep"
			echo "-(--...)   decrement ep"
			echo "           play"
			;;
		-a)
			info

			read -p "Escapism name: " inputname
			while read -p "Episode count: " inputtotal; do
				if [[ -n "$(echo "$inputtotal" | grep -o "[A-Za-z]")" ]]; then
					echo -e "${brown}Episode count can only contain numbers, try again.$foreground"
				else
					break
				fi
			done
			while read -p "Catagory/genre: " inputcatagory; do
				if [[ -z "$(echo "$list" | grep "#" | grep "| $inputcatagory$")" ]]; then
					echo -e "${brown}Catagory $inputcatagory not in list, try again.$foreground"
				else
					break
				fi
			done
			echo ""
			echo -e "Adding $brown$inputname$foreground with $brown$inputtotal$foreground episodes to $brown$inputcatagory$foreground."
			read -p "Is this information correct? [Y/n] " answer
			case "$answer" in
				[Nn*])
					exit
					;;
				*)
					width="$(echo "$inputtotal" | wc -m)"
					count="$((5 - width))"
					space="$(printf " %.0s" $(eval echo "{1..$count}"))"

					sed -i "/#         \| $inputcatagory/a - \[1\/$inputtotal\]$space\| $inputname" "$HOME/etc/neet/list"
					echo ""
					echo "Added $inputcatagory to list."

					# TODO: Finish sort and uncomment this
					# neet -s
					;;
			esac
			;;
		-c)
			# TODO: Finish this
			;;
		-d)			
			debug=true

			echo "info() { "
			info
			echo -e "    active     = $brown$active$foreground"
			echo -e "    name       = $brown$name$foreground"
			echo -e "    current    = $brown$current$foreground"
			echo -e "    total      = $brown$total$foreground"
			echo "}"
			echo ""
	
			echo "fuzzy() {"
			fuzzy
			echo ""
			echo -e "    mostlikely = $brown$mostlikely$foreground"
			echo -e "    count      = $brown$count$foreground"
			echo -e "    path       = $brown$path$foreground"
			echo -e "    currentmpv = $brown$currentmpv$foreground"
			echo "}"
			;;
		-e)
			$EDITOR "$HOME/etc/neet/list"
			;;
		-s)
			info

			count="$(grep -c "#" "$HOME/etc/neet/list")"

			for number in $(eval echo "{1..$count}"); do
				# TODO: Finish this
				echo -e "Sorted $brown$(grep -m $number "#" "$HOME/etc/neet/list" | tail -n 1 | cut -d " " -f 11- | tr -d "\r" | tr -d "\n")$foreground list."
			done

			echo -e "$active\n\n$sort1\n$sort2" > ~/test
			;;
		+(+))
			info
			amount="$(echo "$1" | wc -m)"
			increment="$((current - 1 + amount))"

			shift
			if [[ "$increment" -le "$total" ]]; then
				sed -i "s/+ \[$current\/$total\]/+ \[$increment\/$total\]/" "$HOME/etc/neet/list"
				current="$increment"

				fuzzy
				play
			else
				category="$(echo "$list" | grep -B 32 "+" | grep "#" | tail -n 1 | cut -d "|" -f 2- | cut -c 2- | head -c -1)"

				echo -n -e "$brown$name$foreground completed! Would you like to remove this $category? [y/N] "
				read -p "" answer
				case "$answer" in
					[Yy]*)
						sed -i "/$name/d" "$HOME/etc/neet/list"
						;;
					*)
						exit
						;;
				esac
			fi
			shift
			;;
		+(-))
			info
			amount="$(echo "$1" | wc -m)"
			decrement="$((current + 1 - amount))"

			shift
			if [[ "$decrement" -ge 1 ]]; then
				sed -i "s/+ \[$current\/$total\]/+ \[$decrement\/$total\]/" "$HOME/etc/neet/list"
				current="$decrement"

				fuzzy
				play
			else
				echo "Can't go lower than 1!"
			fi
			shift
			;;
		*)
			echo "Invalid option, use -h for help."
			;;
	esac
else
	info
	fuzzy
	play
fi
